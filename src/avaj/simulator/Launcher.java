package avaj.simulator;

import java.io.*;

class Launcher {

    public static void main(String[] args) {
        if (args.length == 0) {
            System.err.println("Put the file");
            System.exit(0);
        }
        Launcher launch = new Launcher();
    }
}